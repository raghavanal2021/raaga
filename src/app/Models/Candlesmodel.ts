export interface CandlesModel
{
    symbol:String,
    tradedate:Date,
    openPrice:Number,
    highPrice:Number,
    lowPrice:Number,
    closePrice:Number,
    volume:Number,
    prevvolume:Number,
    avgvolume:Number,
    relativeVol:Number,
    pattern:Number,
    CandlePattern:String,
    PatternType:String
}